# 자체 점검 기록

Phase 완료 시점마다 아래 체크리스트로 점검하고 결과를 **누적**한다. 지난 기록은 수정하지 않는다.
체크 실패 항목은 지우지 말고 남긴 뒤, 조치 결과를 다음 Phase 점검에 다시 적는다.
"발견한 문제"에 적은 내용 중 과정에서 배운 것은 `JOURNAL.md`에도 남긴다.

## 점검 항목 (템플릿)

```markdown
## Phase N — YYYY-MM-DD

### 계약 준수
- [ ] SPEC.md의 URI·JSON 필드가 변경되지 않았는가
- [ ] E2E 시나리오(SPEC.md 5절)가 여전히 통과하는가

### 절대 규칙
- [ ] 엔티티에 public setter가 없는가
- [ ] Controller에 엔티티가 노출되지 않았는가
- [ ] 도메인 간 Repository 직접 참조가 없는가
- [ ] Service가 웹 타입(HttpServletRequest, Cookie)에 의존하지 않는가
- [ ] 금액이 전부 BigDecimal인가 (compareTo 사용, equals 금지)

### 품질
- [ ] ./gradlew build 통과
- [ ] 스펙 이탈 항목이 DECISIONS.md에 기록됐는가
- [ ] 근거 없는 설정값(풀 크기, TTL 등)이 남아 있지 않은가

### 발견한 문제
(있으면 기록, 없으면 "없음")
```

---

## Phase 0 — 2026-08-07

### 계약 준수
- [x] SPEC.md의 URI·JSON 필드가 변경되지 않았는가
      8개 엔드포인트 URI·메서드 그대로. 요청/응답 필드명 그대로(`customerId`, `customerPoint`, `productId`, `productName`, `productPrice`, `quantity`, `products`)
- [x] E2E 시나리오(SPEC.md 5절)가 여전히 통과하는가
      curl로 6단계 전부 확인 — 가입 1,000,000 → 로그인 `bff-access` 쿠키 → 상품 3개 → 주문 970,000 → 수량 2 → 취소 985,000

### 절대 규칙
**5개 항목 전부 미준수. 의도된 것이다.** Phase 0은 개선 전 기준점이고, 이 항목들을 고치는 것이 Phase 1의 내용이다
(범위 합의 경위는 JOURNAL.md 2026-08-07 항목).

- [ ] 엔티티에 public setter가 없는가 → **있음**. `@Setter` 3종 (Phase 1: 정적 팩토리 + 행위 메서드)
- [ ] Controller에 엔티티가 노출되지 않았는가 → **노출됨**. `@RequestBody Product`, `@RequestBody Customer` (Phase 1: DTO 분리)
- [ ] 도메인 간 Repository 직접 참조가 없는가 → **있음**. `CustomerService`가 `ProductRepository` 주입 (Phase 1)
- [ ] Service가 웹 타입에 의존하지 않는가 → **의존함**. `CustomerService` → `SessionHandler` → `HttpServletRequest` (Phase 1: `@LoginCustomer`)
- [ ] 금액이 전부 BigDecimal인가 → **아님**. `Double` (Phase 1)

예외: `OrderListDto`·`OrderItemDto`·`OrderRequest`·`CustomerSession`은 Phase 0인데도 별도 클래스다.
SPEC이 요구하는 JSON 모양을 엔티티만으로 만들 수 없고, 강의 자료도 이 4개를 `data/dto/`에 두고 있다.

### E2E가 검출하지 못하는 것

**E2E 시나리오(SPEC.md 5절)는 상품 가격을 변경하지 않는다.** 그래서 가장 큰 결함인
`orderedPrice` 부재(현재가 기준 환불)를 **통과시켜 버린다.** 주문과 취소 사이에 가격이 그대로면
현재가 환불과 주문가 환불의 결과가 같기 때문이다.

즉 "E2E 통과"는 계약 이행의 증거일 뿐 정확성의 증거가 아니다. 가격을 올린 뒤 취소하는
회귀 시나리오는 Phase 1에서 `orderedPrice`를 넣을 때 테스트로 추가한다 (PLAN.md Phase 4).

### 품질
- [x] `./gradlew build` 통과
- [x] 스펙 이탈 항목이 DECISIONS.md에 기록됐는가 — 8절 신설 (스펙이 비워둔 부분의 선택 7건)
- [x] 근거 없는 설정값이 남아 있지 않은가
      HikariCP·Tomcat은 기본값 유지(Phase 3에서 측정 후 조정). `jwt.expiration-ms: 3600000`은 Phase 0 임시값임을 주석에 명시

### 강의 자료 명세 대조 (2026-08-07 추가 수행)

Phase 0 커밋 이후 원본 자료를 발견해 **항목 단위 대조**를 별도로 수행했다.

**대조 범위** — 인쇄 548~556. `ProductService` 5개 메서드, `CustomerService` 8개 메서드,
`ProductController` 5개, `CustomerController` 8개. 슬라이드의 산문(`- 항목`) 한 줄을 하나의 체크 항목으로 옮겨
코드에서 해당 동작의 존재를 직접 확인했다.

**결과**

| 구분 | 건수 | 조치 |
|---|---|---|
| 명세대로 구현됨 | 대부분 (비즈니스 로직 항목 전부) | — |
| 메서드명 불일치 | 9건 | 자료에 맞춰 정정 (`getProducts`→`getAllProducts` 등) |
| Service 반환 타입 불일치 | 전 메서드 | `Response` 반환으로 되돌림. Phase 1에서 다시 걷어낼 예정 |
| 검증 누락 | 2건 | `updateCustomer`의 포인트 검증, `ResponseException` 2-인자 생성자 |
| 메서드 부재 | 1건 | `SessionHandler.storeAccessToken` 추가 (`writeCookie`+`createToken` 통합) |
| 따르지 않기로 한 지침 | 1건 | Product ID `0L` 세팅 — SQL 로그 실측 후 미적용 (DECISIONS.md 10.2) |
| 자료 자체의 오류 | 5건 | DECISIONS.md 10.1에 기록 |

**대조 후 API 계약 재확인** — URI·HTTP 메서드·요청/응답 필드명·파라미터 기본값(`offset=0`, `count=10`)은
8개 엔드포인트 전부 변경 없음. 어긋났던 것은 자바 메서드명이지 계약이 아니다.

### 이식성 검증 (2026-08-07 추가 수행)

"채점자가 클론해서 실행한다"는 전제를 실제로 확인했다. 저장소 밖 임시 경로에 클론해
**원본 개발 컨테이너가 가동 중인 상태로** 격리 실행했다 (조건 상세는 JOURNAL.md 동일자 항목).

| 확인 항목 | 결과 |
|---|---|
| 커밋되지 않았는데 실행에 필요한 파일이 있는가 | 없음. 미추적 파일 0건, ignore 대상은 `.gradle/`·`build/`·IDE·AI 설정뿐 |
| `.env` 등 gitignore된 설정에 의존하는가 | 의존하지 않음. `.env` 없이 저장소 기본값만으로 기동 |
| `gradlew` 실행 권한 | git 인덱스 `100755`, 클론 후 `-rwxr-xr-x` |
| README 실행 방법만으로 기동되는가 | **통과.** 포트 오버라이드도 README에 문서화된 방법 |
| 문서에 없는 수동 조치가 필요했는가 | **없음.** DB 수동 생성·권한 부여·환경변수 파일 생성 모두 불필요 |
| 시드 데이터 삽입 | 정상 3건 |
| E2E 6단계 | 통과 (970,000 → 985,000) |

**발견한 결함 1건 — 즉시 수정** — `docker-compose.yml`의 `container_name` 하드코딩.
호스트 포트는 오버라이드되는데 컨테이너 이름이 고정이라 같은 머신에서 두 번째 인스턴스가
기동하지 못했다. 제거 후 재클론해 재검증 (`ce744c6`).

**검증 조건** — "통과"는 조건과 함께여야 의미가 있다.

| 항목 | 값 |
|---|---|
| 대상 | `ce744c6`을 로컬 경로에서 `git clone` → `/tmp/verify-phase0` |
| 호스트 | macOS Darwin 25.5.0 (Apple Silicon), Docker 29.6.2 / Compose v5.3.1 |
| 동시 상태 | **원본 개발 컨테이너가 5433에서 가동 중인 채로** 격리 실행 |
| 포트 | `POSTGRES_PORT=5434`, `SERVER_PORT=9090` (README 문서화 범위) |
| 볼륨 | 신규 생성. 실행 전 잔존 볼륨 없음을 확인 (이전 실행 데이터로 통과하는 것을 방지) |
| 정리 | `down -v` + 이미지 삭제 + 디렉터리 삭제, 잔존 0건. 원본 무영향 확인 |

**검증하지 않은 범위** — 아래는 이번 검증이 보증하지 못한다.

| 미검증 범위 | 실제 리스크 |
|---|---|
| 다른 물리 머신 | 낮음. 저장소 외부 의존이 없음을 파일 목록으로 확인 |
| 리눅스 호스트 | 중간. 이미지는 리눅스지만 호스트 포트 바인딩 동작이 macOS와 다르다 (JOURNAL 2026-08-06 참조) |
| **오프라인·폐쇄망** | **높음.** 아래 문제 표에 등록 |
| x86_64 아키텍처 | 중간. ARM에서만 빌드했다. `postgres:16-alpine`·`eclipse-temurin`은 멀티아치를 제공하나 확인하지 않음 |
| 반복 기동 | 낮음. 2회 기동 시 시드 중복 없음은 별도 확인함 |

> **[2026-08-08 후속] 위 표의 첫 줄이 틀렸다.** "다른 물리 머신 — 리스크 낮음"으로 적었는데,
> 실제로 옮겨보니 **문서에 없는 조치가 3건 필요했다.** 리스크 판정에서 가장 낮게 본 항목이
> 가장 많은 결함을 냈다. `x86_64`·`다른 물리 머신` 두 줄은 이번에 닫혔고,
> `리눅스 호스트`·`오프라인`은 여전히 열려 있다. 상세는 이 문서 맨 아래
> **"머신 이전 검증"** 절.
>
> 판정 근거가 "저장소 외부 의존이 없음을 **파일 목록으로** 확인"이었다는 점이 핵심이다 —
> 저장소 안만 봤고, **저장소 밖에서 필요한 것**(인터프리터·줄바꿈 변환·데몬)은 세지 않았다.

### 발견한 문제

**대부분 Phase 0 기준점이므로 지금 고치지 않는다.** 해당 Phase에서 처리하고 그때 이 목록을 다시 확인한다.
이미 해결된 항목은 그렇게 표시한다.

| 문제 | 처리 Phase |
|---|---|
| `jwt.secret`이 평문으로 저장소에 커밋됨 | 2 (JWT 하드닝) |
| 쿠키에 HttpOnly·Secure·SameSite 없음 | 2 |
| 비밀번호 평문 저장 (BCrypt 미적용) | 2 |
| ~~목록 조회 응답에 비밀번호 노출~~ | **해소됨** — Phase 1 4단계 DTO 분리. `CustomerResponse`에 필드 자체가 없다 |
| `PUT`/`DELETE`에 본인 확인 없음 (BOLA) | 2 |
| ~~가입 시 `customerPoint`를 클라이언트가 지정 가능 (Mass Assignment)~~ | **해소됨** — Phase 1 4단계. `CustomerRequest`에 `customerPoint` 필드가 없어 받을 방법 자체가 사라졌다 |
| `quantity` 음수 검증 없음 — 주문 시 포인트가 늘어남 | 2 |
| 비즈니스 예외가 전부 HTTP 200 — 클라이언트가 HTTP 레벨에서 성공/실패를 구분할 수 없다. 바디를 파싱해야만 알 수 있어 표준 HTTP 클라이언트·게이트웨이·모니터링이 에러를 못 잡는다 | 2 (Error → 상태 매핑) |
| 음수 포인트 검증 실패가 `DATA_NOT_FOUND`로 나간다 — 검증 실패인데 "데이터를 찾을 수 없음"이라 매핑 시 400이 아닌 **404**가 된다. 자료의 에러 코드 설계가 부정확한 지점으로, 의도적으로 보존한 것 (DECISIONS.md 10·11.3절) | 2 (Error → 상태 매핑) |
| **`offset` 해석 미확정** — 페이지 번호로 구현돼 있어 `offset=1&count=10`이 11번째가 아닌 **101번째**부터 반환한다. 강의 자료에 근거가 없어 외부 확인 대기 | 외부 확인 대기 |
| 고객 삭제 시 주문이 남아 있으면 FK 제약 위반 → 500 | 2 (참조 무결성 정책) |
| 취소 환불이 주문 시점이 아닌 **현재 가격** 기준 | 1 (`orderedPrice` 스냅샷) |
| 고객 주문 조회에서 `item.getProduct()`마다 SELECT (N+1) | 3 |
| 동시 주문 시 포인트 Lost Update 가능 | 3 (`@Version`) |
| 테스트 코드 없음 | 4 |
| `docker-compose.yml`의 `container_name` 하드코딩 — 포트를 바꿔도 같은 머신에서 두 번째 인스턴스가 뜨지 못한다 | **해결됨** (`ce744c6`) |
| **오프라인 환경에서 이미지 빌드 실패** — `Dockerfile`이 빌드 단계에서 `./gradlew dependencies`·`bootJar`를 돌려 Gradle 의존성을 새로 내려받는다. 인터넷이 없으면 `docker compose up`이 빌드에서 멈춘다 | 5 (운영·배포) |
| **채점 환경이 폐쇄망이면 실행 불가** — 위와 같은 원인. 베이스 이미지(`postgres:16-alpine`, `eclipse-temurin`) 풀도 레지스트리 접근이 필요하다. 대응은 사전 빌드된 이미지 배포·오프라인 의존성 캐시 동봉·jar 직접 제출 등이 있으나 **채점 환경을 모르는 상태에서 미리 대응하는 것은 근거 없는 대비**다. Phase 5에서 배포 방식을 정할 때 함께 판단한다 | 5 (운영·배포) |

### Phase 1 진행 중 — 절대 규칙 해소 현황 (2026-08-07, 7단계에서 완료)

Phase 0 점검에서 **5개 전부 미준수**로 기록했던 항목의 현재 상태다.
(위 Phase 0 기록은 그 시점의 사실이므로 수정하지 않는다. 정식 Phase 1 자체 점검은 7단계 완료 후 수행한다.)

| 절대 규칙 | 상태 | 근거 |
|---|---|---|
| 엔티티에 public setter가 없는가 | ✅ 해소 (5단계) | `@Setter` 제거, `@NoArgsConstructor(PROTECTED)`. 엔티티 3종에 setter 0개 |
| Controller에 엔티티가 노출되지 않았는가 | ✅ 해소 (4단계) | 요청·응답 모두 DTO. `@RequestBody Product`·`Customer` 제거 |
| Service가 웹 타입에 의존하지 않는가 | ✅ 해소 (6단계) | `@LoginCustomer` ArgumentResolver로 이동. Service import에 `jakarta.servlet`·`springframework.web`·공통 `Response` 없음 |
| 금액이 전부 BigDecimal인가 | ✅ 해소 (3단계) | `@Column(precision=19, scale=2)`. 비교는 `compareTo`, 생성은 문자열 생성자 |
| 도메인 간 Repository 직접 참조가 없는가 | ✅ 해소 (7단계) | `OrderService` 분리. 각 Service가 자기 Repository만 의존한다 |

### Phase 0 기준점 커밋

Phase 3(N+1·락)·Phase 6(성능) 의 **개선 전/후 비교는 이 시점을 대상으로 한다.**
어느 커밋인지 특정 가능해야 하므로 태그로 고정한다.

```bash
git rev-parse phase0-baseline      # 해시 확인
git show phase0-baseline           # 내용 확인
git diff phase0-baseline..HEAD     # 이후 변경 전체
```

**태그: `phase0-baseline`** — Phase 0 마감 커밋(이 점검 기록을 담은 커밋)에 붙인다.

**왜 `449b74f`(`feat: 스펙 기본 구현 완료`)가 아닌가** — 그 커밋은 원본 자료를 발견하기 전
상태라 클래스명·패키지가 자료와 다르고, 명세된 검증(`isAnyEmpty`, `customerPoint` 유효성)이
누락돼 있었다. **기준점은 "처음 작성한 코드"가 아니라 "Phase 1이 개선을 시작하는 지점"** 이어야
비교가 성립한다. 449b74f를 기준으로 삼으면 이후 측정 차이에 클래스명 정정·검증 보완 같은
성능과 무관한 변경이 섞인다.

기준점이 갖춘 조건:
- SPEC.md 계약 100% 구현, E2E 6단계 + 비즈니스 규칙 8종 통과
- 강의 자료 명세와 클래스·패키지·메서드 시그니처 일치
- 개선 대상 결함이 **의도적으로 보존된 상태** (Double 금액, 자연키 PK, 엔티티 노출,
  현재가 환불, N+1, 락 없음, HTTP 200 응답)
- 클론 후 `docker compose up`만으로 기동 확인

---

## Phase 1 — 2026-08-07

### 계약 준수
- [x] SPEC.md의 URI·JSON 필드가 변경되지 않았는가
      8개 엔드포인트 URI·메서드 그대로. 요청/응답 필드명 그대로.
      **값 표기만 바뀌었다** — 금액이 `BigDecimal(scale=2)`이 되어 `1000000.0` → `1000000.00`.
      필드명이 아니라 값 표기이고 숫자로 파싱하면 동일하다 (DECISIONS.md 3절)
- [x] E2E 시나리오(SPEC.md 5절)가 여전히 통과하는가
      단계마다 21개 항목(6단계 + 회귀 15종)을 돌렸고 7단계 후에도 전부 통과

### 절대 규칙 — 5개 전부 준수

Phase 0에서 **5개 전부 미준수**였던 것이 모두 해소됐다.

- [x] 엔티티에 public setter가 없는가 — `@Setter` 제거, `@NoArgsConstructor(PROTECTED)`. 엔티티 3종 setter 0개
- [x] Controller에 엔티티가 노출되지 않았는가 — 요청·응답 모두 DTO
- [x] 도메인 간 Repository 직접 참조가 없는가 — `OrderService` 분리. 각 Service가 자기 Repository만 의존
- [x] Service가 웹 타입에 의존하지 않는가 — `@LoginCustomer` ArgumentResolver.
      Service import에 `jakarta.servlet`·`springframework.web`·공통 `Response` 없음
- [x] 금액이 전부 BigDecimal인가 — `@Column(precision=19, scale=2)`, 비교는 `compareTo`,
      생성은 문자열 생성자

### 품질
- [x] `./gradlew build` 통과
- [x] 스펙 이탈 항목이 DECISIONS.md에 기록됐는가 — 1·2·3·4·5절 갱신·신설
- [x] 근거 없는 설정값이 남아 있지 않은가 — HikariCP·Tomcat 기본값 유지 (Phase 3에서 측정 후 조정)

### Phase 1로 표시했던 문제 대조

| 문제 (Phase 0 점검 시점) | 상태 |
|---|---|
| 취소 환불이 주문 시점이 아닌 현재 가격 기준 | ✅ 해소 (2단계). 실측 확인 — 개선 전 1,020,000(원금 초과) → 개선 후 985,000 |
| 가입 시 `customerPoint` 지정 가능 (Mass Assignment) | ✅ 해소 (4단계). `CustomerRequest`에 필드가 없다 |
| 목록 조회 응답에 비밀번호 노출 | ✅ 해소 (4단계). `CustomerResponse`에 필드가 없다 |

### Phase 1에서 새로 확인한 것

- **E2E가 검출하지 못하던 결함을 별도 시나리오로 잡았다** — 주문 → 가격 변경 → 취소.
  Phase 0 점검에서 지적했던 사각지대이며, 개선 전/후를 각각 실측해 기록했다 (DECISIONS.md 2절)
- **반올림 경계 검증을 추가했다** — 85,000을 7개에 결제(÷7 나눠떨어지지 않음) 후 1개씩 7회 취소 →
  원금 정확히 복귀. 3·5·7단계에서 각각 재확인했다
- **검증 스크립트 자체의 결함을 발견하고 영향 범위를 재검증했다** — 별도 커밋 `76f0fbd`.
  과거 커밋 5개를 다시 돌려 `phase0-baseline` 포함 전부 18/18 통과를 확인했다

### 발견한 문제

Phase 1에서 새로 생긴 문제는 없다. 아래는 Phase 0에서 이월된 것 중 아직 남은 항목이다.

| 문제 | 처리 Phase |
|---|---|
| `jwt.secret` 평문 커밋 · 쿠키 플래그 없음 · 비밀번호 평문 저장 | 2 |
| BOLA (`PUT`/`DELETE`에 본인 확인 없음) | 2 |
| `quantity` 음수 검증 없음 | 2 |
| 비즈니스 예외가 전부 HTTP 200 · 음수 포인트 검증이 `DATA_NOT_FOUND` | 2 |
| 고객 삭제 시 FK 제약 위반 | 2 |
| `loginCustomer`의 `isAnyEmpty`가 Service에 남아 있음 (요청 형태 검사) | 2 (Bean Validation) |
| N+1 · 락 없음 | 3 |
| `offset` 해석 미확정 | 외부 확인 대기 |
| 테스트 코드 없음 | 4 |
| 오프라인·폐쇄망 실행 불가 | 5 |

### Phase 1 완료 커밋

**태그: `phase1-structure`**

Phase 3(N+1·락)·Phase 6(성능) 비교에서 기준선이 **둘** 필요하다.

| 기준선 | 의미 |
|---|---|
| `phase0-baseline` | 스펙 그대로. 개선 전 원점 |
| `phase1-structure` | 구조 재설계 완료. **성능 개선의 직전 상태** |

Phase 3의 N+1·락 개선은 이 구조 위에서 이뤄지므로, 성능 비교의 직접 대조군은 `phase1-structure`다.
`phase0-baseline`과의 비교는 "구조 재설계가 성능에 미친 영향"을 별도로 보여준다 —
두 기준선을 나눠야 **구조 변경 효과와 성능 최적화 효과가 섞이지 않는다.**

> **[Phase 2에서 갱신]** 위 판단은 Phase 2 시점에 **틀린 것이 됐다.** BCrypt 도입으로 로그인 경로가
> 7.6ms → 69.9ms로 늘어, `phase1-structure`를 대조군으로 쓰면 그 비용이 "성능 저하"로 읽힌다.
> Phase 3의 직접 대조군은 **`phase2-security`** 다 — 아래 "Phase 2 완료 커밋" 참조.
> (기존 기록은 수정하지 않는다는 규칙에 따라 원문을 그대로 두고 덧붙인다. 여기서 배운 것:
> **기준선은 "지금이 마지막 단계"라는 가정 위에서 정하면 다음 Phase가 그 가정을 깬다.**)

---

## Phase 2 진행 중 — 자산 기준 노출 경로 점검 (2026-08-07)

B-3에서 얻은 교훈 — **경로가 아니라 자산에서 출발한다** — 을 적용해 전체를 훑었다.
B-1에서는 "로그인 응답"이라는 **경로 하나**만 보고 사용자 열거를 막았다고 적었는데,
자산(유효한 ID 목록)에서 출발했다면 목록 API가 바로 보였을 것이다.

아래는 **자산마다 그것에 이르는 모든 경로**를 센 결과다. 위협 모델링의 기본 형태이며,
"막았다"는 주장의 범위를 정확히 하기 위한 것이다.

### 자산 1 — 비밀번호

| 경로 | 상태 |
|---|---|
| DB 저장 | ✅ BCrypt 해시 (A-1, DB 직접 조회로 확인) |
| 로그인 응답 | ✅ `CustomerResponse`에 필드 없음 |
| 목록 조회 응답 | ✅ 같은 DTO |
| 엔티티 실수 직렬화 | ✅ `@JsonIgnore` (B-4). 단 도달 경로가 없어 E2E 미확인 — Phase 4 단위 테스트 |
| 로그 | ⚠️ **미점검.** AOP 로깅이 Phase 5에 있고, 요청 바디를 찍으면 평문이 남는다. Phase 5에서 마스킹 |

### 자산 2 — 유효한 customerId 목록

| 경로 | 상태 |
|---|---|
| 로그인 응답 차이 | ✅ 401로 통일 (B-1) |
| BOLA 응답 차이 (403 vs 404) | ✅ 소유권 검사를 존재 확인보다 먼저 (B-3) |
| `GET /api/customers/list` | ❌ **열려 있다.** 익명으로 전체 ID 획득 가능 |
| 로그인 응답 시간 차이 | ⚠️ 없는 ID는 BCrypt를 건너뛰어 더 빠르다 (9-3절) |

> **이 자산은 막히지 않았다.** `/list` 하나로 전체가 나간다. 다른 세 경로를 막은 것은
> "그 경로로는 안 된다"는 의미일 뿐, **자산이 보호된다는 뜻이 아니다.**
> 완전한 차단은 역할 기반 접근 제어가 전제다 (미적용, 아래 문제 표).

### 자산 3 — 고객의 포인트 잔액

| 경로 | 상태 |
|---|---|
| 본인 조회 | ✅ 정상 기능 |
| 타인 조회 `GET /{customerId}` | ✅ 403 (B-3) |
| `GET /api/customers/list` | ❌ **열려 있다.** 전체 고객의 잔액이 익명 노출 |

### 자산 4 — 고객의 주문 이력

| 경로 | 상태 |
|---|---|
| 타인 조회 `GET /{customerId}` | ✅ 403 (B-3) |
| `GET /api/customers/list` | ✅ 목록에는 주문이 포함되지 않는다 (`customerId` + `customerPoint`만) |

### 자산 5 — 포인트 잔액의 무결성 (증감 경로)

"포인트는 음수가 될 수 없다"·"공짜로 늘지 않는다"에 이르는 경로를 셌다.

| 경로 | 상태 |
|---|---|
| `usePoint` (주문) | ✅ 잔액 검사 + 양수 검사 |
| `refundPoint` (취소) | ✅ 양수 검사 (B-2에서 발견 — **잔액 검사가 없어 음수 환불로 우회 가능했다**) |
| `changePoint` (정보 수정) | ✅ 음수 거부 + BOLA 방어 (B-2·B-3) |
| 음수·0 수량 주문·취소 | ✅ `@Positive` + 엔티티 불변식 이중 방어 (B-2) |
| 동시 요청 | ❌ **열려 있다.** 낙관적 락 미적용 — Phase 3 |

> 자산 5는 **B-2에서 자산 기준으로 훑은 덕에** `refundPoint` 구멍을 찾았다.
> `@Positive`만 붙이고 끝냈으면 주문 경로만 막고 취소 경로는 남았을 것이다.

### 정리

**"막았다"고 말할 수 있는 자산**: 비밀번호(로그 제외), 주문 이력, 포인트 무결성(동시성 제외)
**막히지 않은 자산**: 유효한 ID 목록, 포인트 잔액 — 둘 다 `GET /api/customers/list` 하나 때문이다

### Phase 2로 표시했던 문제 대조

| 문제 (Phase 0·1 점검 시점) | 상태 |
|---|---|
| 비밀번호 평문 저장 | ✅ 해소 (A-1) BCrypt. DB 직접 조회로 확인 |
| `jwt.secret` 평문 커밋 | ✅ 해소 (A-2) 환경변수. 히스토리 잔존은 DECISIONS 12절에 구분 기록 |
| 쿠키에 HttpOnly·Secure·SameSite 없음 | ✅ 해소 (A-3) `Secure`는 `COOKIE_SECURE`로 제어 |
| 비즈니스 예외가 전부 HTTP 200 | ✅ 해소 (B-1) SPEC 4절 매핑 |
| 음수 포인트 검증이 `DATA_NOT_FOUND` | ✅ 해소 (B-1) `ParameterException` 400 |
| `quantity` 음수 검증 없음 | ✅ 해소 (B-2) `@Positive` + 엔티티 불변식 |
| `loginCustomer`의 `isAnyEmpty`가 Service에 | ✅ 해소 (B-2) `@Valid` + `@NotBlank`로 Controller 이동 |
| BOLA (`PUT`/`DELETE` 본인 확인 없음) | ✅ 해소 (B-3) `NOT_OWNER` 403 |
| 고객 삭제 시 FK 제약 위반 → 500 | ✅ 해소 (B-5) `DATA_IN_USE` 409 |

### Phase 2에서 새로 발견해 처리한 것

| 발견 | 처리 |
|---|---|
| **사용자 열거** — 로그인 실패가 404/401로 갈려 계정 존재 노출 | 401로 통일 (B-1). 단 `/list`로 여전히 열려 있음 |
| **음수 환불로 잔액 검사 우회** — `refundPoint`에 검사가 없어 잔액이 음수까지 갈 수 있었다 | 양수 검사 추가 (B-2) |
| **fail-late** — 짧은 JWT 시크릿으로도 앱이 뜨고 첫 로그인에서 500 | `@PostConstruct`로 기동 시 검증 (A-2) |
| **깨진 JSON이 500** | `HttpMessageNotReadableException` 핸들러 → 400 (B-1) |
| **BOLA 응답이 계정 존재를 노출할 뻔** | 소유권 검사를 존재 확인보다 먼저 (B-3) |

### 남은 문제

| 문제 | 처리 |
|---|---|
| **`GET /api/customers/list`가 익명 공개** — 전체 `customerId` + 잔액 노출. 역할 기반 접근 제어가 전제라 이번 Phase 범위 밖. 계약 유지 결정 (DECISIONS 9-5절) | 미해결 (역할 도입 시) |
| 로그인 응답 시간으로 계정 존재 추정 (타이밍) | 미해결 (rate limiting 선행) |
| 요청 속도 제한 없음 | 미적용 |
| 요청 바디 로깅 시 비밀번호 평문 노출 가능 | 5 (AOP 로깅 + 마스킹) |
| `@JsonIgnore` 방어를 E2E로 확인 불가 | 4 (엔티티 직렬화 단위 테스트) |
| N+1 · 락 없음 | 3 |
| `offset` 해석 미확정 | 외부 확인 대기 |
| 테스트 코드 없음 | 4 |
| 오프라인·폐쇄망 실행 불가 | 5 |


---

## Phase 2 — 2026-08-07

### 계약 준수
- [x] SPEC.md의 URI·JSON 필드가 변경되지 않았는가
      URI·메서드·필드명 그대로. **HTTP 상태 코드가 200에서 SPEC 4절 매핑으로 바뀌었고**(B-1),
      `GET /api/customers/{customerId}`에 인증 요구가 추가됐다(B-3). 둘 다 SPEC.md에 반영했다 —
      문서가 코드와 다르면 그 자체가 결함이다
- [x] E2E 시나리오(SPEC.md 5절)가 여전히 통과하는가 — 48개 항목 전부 통과

### 절대 규칙 — 5개 유지
Phase 1에서 충족한 상태가 유지되는지 확인했다. 새 코드(`LoginCustomerArgumentResolver`,
`PasswordConfig`)는 `global/`에 두었고 도메인 경계를 넘지 않는다.
- [x] 엔티티에 public setter 없음
- [x] Controller에 엔티티 미노출
- [x] 도메인 간 Repository 직접 참조 없음 — 참조 무결성 확인도 DB에 위임해 경계를 지켰다(B-5)
- [x] Service가 웹 타입에 의존하지 않음
- [x] 금액 BigDecimal

### 품질
- [x] `./gradlew build` 통과
- [x] 스펙 이탈 항목이 DECISIONS.md에 기록됐는가 — 9-1 ~ 9-6절 신설
- [x] 근거 없는 설정값이 남아 있지 않은가
      BCrypt work factor는 기본값 10을 쓰고 "근거 없이 바꾸지 않는다"를 명시.
      토큰 만료 1시간은 임시값에서 근거 있는 선택으로 바꿨다(9-2절)

### Phase 2 완료 커밋 — 기준선이 셋이 된 이유

**태그: `phase2-security`**

| 기준선 | 의미 |
|---|---|
| `phase0-baseline` | 스펙 그대로. 개선 전 원점 |
| `phase1-structure` | 구조 재설계 완료 |
| `phase2-security` | 보안 강화 완료. **Phase 3 개선의 직접 대조군** |

**Phase 2에서 BCrypt가 도입되어 로그인 경로의 응답 시간이 구조적으로 늘어났다.** 해싱은
의도적으로 느린 연산이므로 이것은 결함이 아니라 **보안 강화의 비용**이다. 그 비용이 Phase 3의
성능 개선 효과와 섞이면 두 수치를 모두 못 쓰게 된다.

**실측** (같은 머신·같은 DB·5회 평균, curl `time_total`):

| 경로 | `phase1-structure` | `phase2-security` | 차이 |
|---|---|---|---|
| 로그인 | 0.0076s | **0.0699s** | **약 9배 (+62ms)** |
| 회원가입 | 0.116s | 0.191s | +75ms |
| 주문 조회 (대조군) | 0.0054s | 0.0081s | 노이즈 수준 |

로그인·가입만 늘고 **BCrypt와 무관한 주문 조회는 사실상 동일**하다. 즉 증가분의 출처가 해싱임이
대조군으로 확인된다.

Phase 6에서 로그인이 포함된 시나리오를 측정할 때 `phase1-structure`를 대조군으로 쓰면
**BCrypt 비용이 "성능 저하"로 잘못 읽힌다.** `phase0-baseline`과의 비교는 "전체 개선 여정",
`phase1-structure`와의 비교는 "구조 재설계 효과", `phase2-security`와의 비교는 "성능 최적화 효과"를
각각 보여준다 — **섞이지 않게 나누는 것이 기준선을 셋으로 두는 이유다.**

> 측정 조건은 개발 노트북의 Docker 컨테이너이며 절대 수치는 환경에 좌우된다(DECISIONS.md 13절).
> 여기서 의미 있는 것은 **경로 간 상대 차이**와 **대조군이 움직이지 않았다는 사실**이다.

---

## Phase 3 — 2026-08-07

### 계약 준수
- [x] SPEC.md의 URI·JSON 필드가 변경되지 않았는가 — 변경 없음
- [x] E2E 시나리오가 여전히 통과하는가 — **48/48**
- 새 응답 상태 하나 추가: 낙관적 락 충돌 → **409 `CONCURRENT_MODIFICATION`**.
  `SPEC.md` 4절이 이미 "낙관적 락 충돌 → 409"로 예고한 자리이므로 계약 이탈이 아니다

### 절대 규칙
- [x] 엔티티에 public setter 없음 — `@Version` 필드는 JPA가 관리하며 setter 없음
- [x] Controller에 엔티티 미노출
- [x] 도메인 간 Repository 직접 참조 없음
- [x] Service가 웹 타입에 의존하지 않음
- [x] 금액 BigDecimal — 정합성 단언도 `compareTo` 비교자를 명시해 사용
- [x] `open-in-view: false` 유지

### 품질
- [x] `./gradlew build` 통과 — **테스트 6건, 실패 0**
- [x] 스펙 이탈·설계 판단이 DECISIONS.md에 기록됐는가 — 14절 신설
- [x] 근거 없는 설정값이 없는가 — 캐싱·풀 튜닝을 **Phase 6으로 미룬 것 자체가** 이 규칙의 적용이다.
      k6 없이 정하면 "측정 결과나 명시적 근거"를 붙일 수 없다

### 이 Phase의 성격 — 측정이 결과물이다

Phase 3은 코드 변경량이 작다(`@EntityGraph` 1줄, `@Version` 1필드, 핸들러 1개).
**결과물은 코드가 아니라 그 변경이 옳다는 근거**이며, 그래서 측정 장치를 먼저 검증하는 데
시간을 더 썼다.

| 측정 | 개선 전 | 개선 후 |
|---|---|---|
| `GET /api/customers/{id}` 쿼리 수 (상품 20종) | 22 | **2** |
| 동시 주문 100건 후 포인트 오차 | **88,000** | **0** |

### 측정 장치가 먼저 틀린 사례 — 4건

이 Phase에서 **측정 대상보다 측정 장치가 더 자주 틀렸다.**

| # | 무엇이 | 어떻게 보였는가 | 무엇이 잡았는가 |
|---|---|---|---|
| 1 | grep 패턴이 로거명과 불일치 | SQL 0개 | 0이 명백히 틀린 값 |
| 2 | 측정 픽스처가 DB를 오염 | 대조군이 2 → 1로 변동 | **대조군** |
| 3 | 스레드 배리어 교착 | 소요 30,213ms | 타임아웃 값과 일치 |
| 4 | TPS 비교의 분모 불일치 | 낙관적 락이 더 빠름 | 성공 14/100 |

**1·3·4는 개선 후였다면 성과처럼 보였을 것이다.** 0개 쿼리는 완벽한 N+1 해결로, 높은 TPS는
빠른 락으로 읽힌다. 2만이 "나빠 보이는" 신호였고, 그래서 유일하게 **찾지 않아도 스스로 드러났다.**

> `REVIEW.md`의 "검증하는 쪽을 검증하지 않았다"가 Phase 3에서 **네 번 재발했고 네 번 모두 잡혔다.**
> 잡힌 이유는 이번엔 **미리 방어를 설계해뒀기 때문**이다 — 대조군, 행 수 선단언, 배리어 도달 단언.
> Phase 2까지는 사후에 발견했고, Phase 3에서는 **함정이 발동할 자리에 미리 센서를 놓았다.**

### 새로 발견해 처리한 것

- **Docker Engine 29가 구버전 API를 거부해 Testcontainers가 뜨지 못했다.**
  `docker info`·`docker compose`는 정상이라 Docker 문제로 보이지 않았다.
  `api.version=1.44`로 해결. Testcontainers 버전 상향(1.20.6→1.21.3)은 **효과가 없어 되돌렸다**
- **`test` 프로파일에 `jwt.secret`이 없었다.** 첫 테스트를 쓰기 전까지 드러나지 않았다 —
  `local`·`docker`만 값을 갖고 있었다
- **Lost Update의 손실량이 커넥션 풀 크기에 종속된다.** 스레드 100개라도 DB 트랜잭션은
  풀 크기(기본 10)만큼만 동시 진행되므로 경합 폭이 고정된다. 절대값을 결론으로 쓰지 않는다

### 남은 문제

| 문제 | 상태 |
|---|---|
| `GET /api/customers/list` 익명 노출 | ❌ 계약 유지. 역할 기반 접근 제어 필요 (Phase 2에서 기록) |
| 로그인 타이밍 부채널 | ❌ 미해결 |
| 요청 속도 제한 없음 | ❌ 미해결 |
| 캐싱·HTTP 캐시 헤더·풀 튜닝 | ⏸ **Phase 6으로 보류** (측정 근거 필요) |
| 자동 재시도(`@Retryable`) | ⏸ 보류. 재시도 정책은 충돌 분포 측정 위에서 정한다 (`DECISIONS.md` 14절) |
| 재고 도입 시 락 전략 재검토 | ⏸ 상품 행 경합이 생기면 결론이 바뀐다 |
| `Product`·`OrderItem`에 `@Version` 없음 | ✅ 의도적. 동시에 갱신되는 필드가 없다 |

### Phase 3 완료 커밋

**태그: `phase3-performance`**

Phase 6 부하 측정의 대조군은 이 태그다. `phase2-security`와의 비교가 "성능 최적화 효과"를 보여준다.

---

## Phase 4 — 2026-08-07

### 계약 준수
- [x] SPEC.md의 URI·JSON 필드가 변경되지 않았는가 — **변경 없음**
      주문 엔드포인트를 `order` 도메인으로 옮겼으나 **URI·메서드·요청/응답 형식은 그대로**다.
      바뀐 것은 그 코드가 어느 패키지에 있는가뿐이다
- [x] E2E 시나리오가 여전히 통과하는가 — curl 48/48, `ShopScenarioTest` 7/7

### 절대 규칙 — 이제 테스트가 강제한다
Phase 3까지는 **현재 성실함에만 기대고 있었다.** 이제 어기면 빌드가 깨진다.

| 규칙 | 강제 방법 |
|---|---|
| 엔티티에 public setter 없음 | ArchUnit (public 필드도 함께) |
| Controller에 엔티티 미노출 | ArchUnit |
| 도메인 간 Repository 직접 참조 없음 | ArchUnit (도메인 3개 각각) |
| Service가 웹 타입에 의존하지 않음 | ArchUnit |
| Service가 `Response`를 반환하지 않음 | ArchUnit |
| 필드 주입 금지 | ArchUnit |
| 도메인 단방향 (`order →` customer/product) | ArchUnit 순환 검사 |

### 품질
- [x] `./gradlew build` 통과 — **테스트 119건, 실패 0**. 커버리지 게이트 포함
- [x] 설계 판단이 DECISIONS.md에 기록됐는가 — 15·16·17절 신설
- [x] 근거 없는 설정값이 없는가 — 커버리지 임계치의 근거를 16절에 적었고,
      **"숫자 자체에 이론적 근거는 없다"는 사실도 함께 적었다**

### 이 Phase가 실제로 산출한 것

테스트 건수(31 → 119)가 아니라 **세 가지 발견**이다.

| # | 발견 | 어떻게 |
|---|---|---|
| 1 | **위조 토큰을 받아들이는 회귀가 테스트 104건을 통과했다** | 커버리지 93%인 클래스에 변이 주입 |
| 2 | **문서가 세 Phase 동안 사실이 아니었다** (customer ↔ order 순환) | ArchUnit 순환 검사 첫 실행 |
| 3 | **내가 쓴 주석이 사실과 반대였다** (`@WebMvcTest` 슬라이스 포함 범위) | 주석대로 안 되길래 확인 |

셋 다 **사람이 읽어서는 안 나왔다.** 1은 숫자가 안심시켰고, 2는 문서가 안심시켰고,
3은 내가 직접 쓴 설명이 안심시켰다.

### 역검증 — 32건 전부

| 대상 | 변이 | 결과 |
|---|---|---|
| 도메인 | 9건 (경계값, 검증 제거, 반올림 방식, 총액 누적 누락 등) | 전부 잡힘 |
| 계층 | 12건 (사용자 열거 부활, 소유권 순서, `@Valid` 제거, 핸들러 제거 등) | 전부 잡힘 |
| 아키텍처 | 10건 (각 규칙을 일부러 위반) | 전부 잡힘 |
| 커버리지 게이트 | 1건 (임계치 0.95로 상향) | 실패함 |

> **한 건만 잡히지 않았다** — `SessionHandler` 위조 토큰. 그것을 계기로 8건을 추가했고
> 재실행에서 3건이 잡았다. **역검증에서 잡히지 않은 것이 이 Phase에서 가장 값진 기록이다.**

### 측정

- 테스트 119건: 벽시계 **73~78초**, 테스트 실행 합계 **3.2초**
- `.withReuse(true)`는 **효과가 없었다** (켬 77·78초 / 끔 73초). 병목은 컨테이너가 아니라
  Spring 컨텍스트 생성(8~9개)이다. 개발자에게 홈 설정을 요구할 근거가 없어 조치하지 않는다
- 커버리지 라인 **89.4%** (262/293) · 분기 **87.2%** (68/78)

### 남은 문제

| 문제 | 상태 |
|---|---|
| `GET /api/customers/list` 익명 노출 | ❌ 계약 유지. **테스트로 현재 계약을 고정**했다 — 인증을 걸면 그 테스트가 깨지며 계약 변경임을 알린다 |
| 로그인 타이밍 부채널 | ❌ 미해결 |
| 요청 속도 제한 없음 | ❌ 미해결 |
| `CustomerService` 라인 63% | ⚠️ `getAllCustomers`·`updateCustomer` 경로가 얕다. 게이트는 통과하나 **얕은 곳으로 인지**한다 |
| 캐싱·풀 튜닝 | ⏸ Phase 6 |
| 테스트 실행 시간 | ⏸ 컨텍스트 수를 줄이는 쪽으로. 지금은 문제 아님 |

### Phase 4 완료 커밋

**태그: `phase4-testing`**

테스트 코드는 런타임에 영향이 없으므로 Phase 6 측정의 대조군은 **`phase3-performance`가 그대로 유효**하다.
다만 이 Phase에서 **운영 코드가 두 번 바뀌었다** —
① 주문 엔드포인트의 패키지 이동(동작 동일, 빈 이름만 변경),
② `lombok.config` 추가(생성 코드에 어노테이션만 붙음).
둘 다 요청 처리 경로의 로직을 바꾸지 않으므로 성능 대조군 판단에는 영향이 없다.

### Phase 4 후속 — 예외 경로 전수 조사 (2026-08-07)

Phase 5 착수 전, `SessionHandler` 구멍이 **표본 조사의 결과**였으므로 같은 형태가 더 있는지
전수로 확인했다.

**방법** — 두 종류를 나눠 봤다.
1. **안 밟힌 곳**: JaCoCo XML의 **라인 단위** 데이터로 `throw`/`catch` 23곳 대조
2. **밟혔는데 단언 없는 곳**: 예외 경로에 변이 17건 주입

**결과** — 3건이 미검증이었다.

| 구멍 | 심각도 | 원래 결정 |
|---|---|---|
| 500 응답에 예외 메시지 유출 | **높음** (정보 유출) | Phase 2 B-1에서 처리한 항목 |
| `ParameterException` 400 → 500 | 중간 | Phase 2 B-2 |
| `supportsParameter` 타입 검사 제거 | 낮음 (방어적 검사) | Phase 1 6단계 |

**세 건 모두 커버리지가 라인 단위로는 이미 "미실행"이라고 알려주고 있었다.**
`LoginCustomerArgumentResolver`는 **라인 100%인데 분기 1/2**였다 —
클래스 백분율 하나로 뭉갠 것이 문제였지 도구가 무용한 것이 아니었다 (`DECISIONS.md` 16절 정정).

**조치** — 운영 코드는 변경하지 않고 테스트 9건 추가.
`GlobalExceptionHandlerTest` 4건 · `LoginCustomerArgumentResolverTest` 5건.
역검증 4건 전부 잡혔다.

**남은 것** — 아직 밟히지 않는 경로가 있다 (게이트는 통과한다).

| 위치 | 상태 |
|---|---|
| `CustomerService.updateCustomer` 본문 | 미실행. Service 성공 경로 테스트 없음 |
| `CustomerService.getAllCustomers` | 미실행 |
| `Customer.changePassword` 성공 경로 | 미실행 |
| `SessionHandler`의 `!cookieSecure` 경고 분기 | 분기 1/2 (로깅) |
| `CustomerService.findCustomer(null)` | 미실행 |

**의도적으로 남긴다.** 이 목록은 "덮어야 할 빚"이 아니라 **다음에 어디를 볼지의 지도**다.
Phase 5에서 운영 코드가 바뀌는 자리(Actuator·Swagger·AOP 로깅)와 겹치는 것이 있으면
그때 함께 본다. 지금 커버리지를 채우려고 테스트를 쓰면 Phase 4에서 경계한
"테스트를 위한 테스트"가 된다.

**이 조사에서 얻은 절차** — 다음 Phase부터 마감 시 이 순서로 확인한다.
1. JaCoCo 리포트를 **클래스 백분율이 아니라 미실행 라인·부분 분기 목록**으로 연다
2. 그 목록이 **의도적으로 비운 것인지** 확인한다 (의도적이면 사유를 적는다)
3. 목록에 없는데 확신이 필요한 곳만 변이를 넣는다 — 범위가 훨씬 좁아진다

---

## Phase 5 — 2026-08-07

### 계약 준수
- [x] SPEC.md의 URI·JSON 필드가 변경되지 않았는가 — **변경 없음**
- [x] E2E 시나리오가 여전히 통과하는가 — curl 48/48 (로컬·compose·클론 모두)
- 요청 DTO에 `@JsonProperty(WRITE_ONLY)`를 붙였으나 **요청 파싱은 그대로**다.
  이 DTO들은 응답으로 나가지 않으므로(응답은 `CustomerResponse`) 계약에 영향이 없다
- 응답에 `X-Trace-Id` 헤더가 추가됐다. **바디는 그대로**이므로 계약 이탈이 아니다

### 절대 규칙
- [x] 엔티티에 public setter 없음 — `BaseTimeEntity`의 감사 필드도 setter 없이
      리스너가 리플렉션으로 채운다. 별도 테스트로 고정
- [x] Controller에 엔티티 미노출 / 도메인 간 Repository 직접 참조 없음 /
      Service가 웹 타입에 의존하지 않음 — ArchUnit 12건 그대로 통과
- [x] 금액 BigDecimal · `open-in-view: false` 유지

### 품질
- [x] `./gradlew build` 통과 — **테스트 155건, 실패 0**, 커버리지 게이트 포함
- [x] 설계 근거가 DECISIONS.md에 기록됐는가 — 19~23절 신설
- [x] 근거 없는 설정값이 없는가 — `timeout-per-shutdown-phase: 20s`,
      `MaxRAMPercentage=75`, `stop_grace_period: 30s` 모두 이유를 주석에 병기

### 자산 점검 갱신 — 비밀번호의 로그 경로

Phase 2에서 **"로그 경로 미점검"** 으로 남긴 항목을 닫는다.
그때 점검하지 못한 이유는 **로그에 요청 바디를 찍는 코드가 없었기 때문**이다 — 없는 경로는 점검할 수 없다.
API 로깅이 그 경로를 만드는 시점이 Phase 5다.

| 경로 | 상태 |
|---|---|
| 응답 바디 | ✅ 응답 DTO에 필드 없음 + 엔티티 `@JsonIgnore` (Phase 2) |
| **API 로그(AOP)** | ✅ **DTO `@JsonProperty(WRITE_ONLY)` + `SensitiveDataMasker` 이중 방어** |
| 예외 메시지 → 500 응답 | ✅ traceId만 노출 (Phase 2 B-1, Phase 4에서 테스트로 고정) |
| Actuator `/env`·`/heapdump` | ✅ 닫혀 있음 (테스트로 고정) |
| SQL 로그 바인딩 파라미터 | ⚠️ 기본 비활성. 켜면 해시가 찍힌다 — 주석으로 경고 |

> **"막았다"고 말할 수 있는 자산**에서 비밀번호의 `(로그 제외)` 단서를 뗀다.
> 다만 **로그 캡처 테스트가 함께 있어야만** 그렇게 말할 수 있다 —
> 첫 수동 검증은 거짓 음성이었다(로그가 아무것도 안 남기고 있었다).

### 새로 발견해 처리한 것

| # | 발견 | 계기 |
|---|---|---|
| 1 | **404·405·415·타입불일치가 전부 500 + ERROR 로그** | Actuator의 닫힌 엔드포인트를 두드려보다 |
| 2 | **마스킹 검증이 거짓 음성** — 로그가 아무것도 안 남기고 있었다 | 로그 전문을 확인하다 |
| 3 | **마스커가 `authorization=Bearer <token>`의 토큰을 놓침** | 단위 테스트 |
| 4 | **HEALTHCHECK가 매번 실패** — `/bin/sh`가 dash라 `/dev/tcp` 불가 | `docker inspect` Health.Log |
| 5 | **JPA Auditing이 `data.sql`과 슬라이스 테스트를 깸** | NOT NULL 제약 |

1번은 Phase 2 이후 줄곧 있던 결함이다. **테스트가 전부 실재하는 경로만 호출**해서
세 Phase 동안 드러나지 않았다 — Phase 4 후속 조사에서 세운 "예외 경로를 전수로 본다"의
연장선이지만, 이번엔 **애플리케이션 밖(존재하지 않는 URL)** 이라 그때도 놓쳤다.

### 측정하지 못한 것

**graceful shutdown의 효과를 증명하지 못했다.** 세 가지 실험 모두 graceful과 immediate를
변별하지 못했고, 마지막 실험은 측정 도구 자체가 실패했다(curl 프로세스 기동 > SIGTERM).
확인된 것은 로그(`Commencing graceful shutdown`이 graceful에서만)뿐이다.
원인은 이 API의 요청이 밀리초 단위라 "진행 중"인 창이 도구 오버헤드보다 좁기 때문이다.
**기능이 켜진 것과 효과가 있는 것은 다르고, 후자는 미검증으로 남긴다.**

### 이식성 재검증

Dockerfile과 compose가 바뀌었으므로 Phase 0의 검증을 다시 했다.

- 저장소 밖 클론에서 `POSTGRES_PORT=15433 SERVER_PORT=18080 docker compose up --build`
- **원본 스택(8080)을 켜둔 채 동시 기동** — health `healthy`, E2E **48/48**, Swagger 302
- 양쪽 모두 정상 동작 확인 후 클론 정리

### 남은 문제

| 문제 | 상태 |
|---|---|
| **HA 다중 인스턴스 (app 2개 + LB)** | ⏸ **Phase 5 미완.** 아래 참조 |
| `GET /api/customers/list` 익명 노출 | ❌ 계약 유지 (테스트로 고정) |
| 로그인 타이밍 부채널 / 요청 속도 제한 없음 | ❌ 미해결 |
| graceful shutdown 효과 미측정 | ⚠️ 기능은 활성, 효과는 미검증 |
| `ddl-auto: create` — 롤백 시 데이터 소실 | ⚠️ 학습 프로젝트라 유지. 근거는 `DECISIONS.md` 23절 |
| 오프라인·폐쇄망 | ✅ **판단 완료** — 미리 대비하지 않고 절차만 문서화 (22절) |

> **HA 다중 인스턴스는 이번 Phase에서 하지 않았다.** 무상태(JWT) 설계와 traceId 이어받기 등
> 전제 조건은 갖췄으나, 로드밸런서 구성은 compose 구조를 바꾸고 포트 정책·헬스체크 연동을
> 다시 정해야 해서 범위가 크다. **Phase 6의 부하 측정과 함께 하는 편이 자연스럽다** —
> 다중 인스턴스의 값은 처리량으로 보여야 하고, 그 측정 도구가 Phase 6에서 준비된다.

### Phase 6 대조군 판단

**결론 — Phase 6의 직접 대조군은 `phase5-operations`다. `phase3-performance`는 더 이상 유효하지 않다.**

근거는 실측이다 (같은 머신·같은 DB·30회 평균).

| 경로 | `phase3-performance` | `phase5-operations` | 차이 |
|---|---|---|---|
| 상품 목록 (인증 X) | 3.5ms | 3.6ms | +0.1ms (노이즈) |
| **주문 조회 (인증 O)** | **4.1ms** | **5.3ms** | **+1.2ms (+29%)** |
| 로그인 | 68.8ms | 66.6ms | BCrypt가 지배, 노이즈 |

AOP 로깅은 **모든 요청에서 인자를 JSON으로 직렬화**하고, MDC 필터가 요청마다 추가되며,
엔티티마다 감사 컬럼이 늘었다. 절대값은 작지만 **요청당 상시 비용**이고,
주문 조회처럼 짧은 경로에서는 상대적으로 크게 나타난다.

Phase 6에서 커넥션 풀·캐싱을 튜닝할 때 `phase3-performance`를 대조군으로 쓰면
**운영 계측 비용이 튜닝 효과에 섞인다.** Phase 2에서 BCrypt 때문에 기준선을 나눈 것과 같은 형태다.

| 기준선 | 무엇을 보여주는가 |
|---|---|
| `phase0-baseline` | 전체 개선 여정 |
| `phase1-structure` | 구조 재설계 효과 |
| `phase2-security` | Phase 3 성능 개선의 대조군 |
| `phase3-performance` | N+1·락 개선 완료 시점 |
| `phase4-testing` | 런타임 무영향 (테스트만) |
| **`phase5-operations`** | **Phase 6 튜닝의 직접 대조군** |

> `phase4-testing`은 대조군 후보가 아니다 — 테스트 코드는 런타임에 영향이 없고,
> 그 Phase의 운영 코드 변경(패키지 이동, `lombok.config`)도 요청 처리 로직을 바꾸지 않았다.

### Phase 5 완료 커밋

**태그: `phase5-operations`**

---

## Phase 6 진행 중 — 측정 장치 점검 (2026-08-08 기록)

**Phase 6은 완료되지 않았다.** 이 절은 완료 점검이 아니라 **중단 시점의 상태 기록**이다.
Phase 2의 "진행 중 — 자산 기준 노출 경로 점검"과 같은 성격이다.

> **기록 시점 주의** — 아래 내용은 작업 당시가 아니라 **다른 머신에서 이어받으며 사후에 옮겨 적었다.**
> 근거는 커밋 `99ff4db`·`278b077`·`8a59698`의 메시지와 `docs/evidence/perf/`의 원시 출력이다.
> 작업 당시 `REVIEW`·`JOURNAL`·`DECISIONS` 어디에도 기록되지 않았다는 것 자체가 이번 Phase의 결함이다.

### 측정 장치가 먼저 틀린 사례 — Phase 3의 4건에 이어서

Phase 3에서 "측정 대상보다 측정 장치가 더 자주 틀렸다"며 4건을 표로 남겼다. **Phase 6에서 2건이 더 나왔다.**
같은 형식으로 잇는다.

| # | 무엇이 | 어떻게 보였는가 | 무엇이 잡았는가 |
|---|---|---|---|
| 1 | grep 패턴이 로거명과 불일치 *(P3)* | SQL 0개 | 0이 명백히 틀린 값 |
| 2 | 측정 픽스처가 DB를 오염 *(P3)* | 대조군이 2 → 1로 변동 | **대조군** |
| 3 | 스레드 배리어 교착 *(P3)* | 소요 30,213ms | 타임아웃 값과 일치 |
| 4 | TPS 비교의 분모 불일치 *(P3)* | 낙관적 락이 더 빠름 | 성공 14/100 |
| **5** | **`countStatus` 삽입 오류** — 한 엔드포인트에 3번, 둘에 0번 | **`resp_2xx`가 `http_reqs`와 완전 일치.** "모든 응답이 2xx인데 실패율 0.04%"라는 모순 | 엔드포인트별 태그 분리 (**세 번 헛돈 뒤**) |
| **6** | **부하 모델과 측정 목적 불일치** — `constant-vus`로 유한 자원을 측정 | "동시 주문 정합성" 이름으로 **잔액 소진**을 재고 있었다. 응답의 79%가 `INSUFFICIENT_FUNDS` | 성공 응답 내용 확인 |
| **7** | **측정 대상이 실행되지 않음** — `load.js`가 캐시된 엔드포인트를 부르지 않는다 | 캐시 on/off 차이 0.3% → "이득 없음"으로 읽혔다 | 결론 직전 시나리오 파일을 열어봄 |
| **8** | **부하가 서버를 포화시키지 못함** — `sleep(0.1)`로 327 req/s에 묶임 | 서버측 개선이 드러날 수 없는 조건 | 용량(1,000+)과 대조 |
| **9** | `MSYS_NO_PATHCONV` 전역 export가 **python 인자까지** 변환 차단 | `can't open file 'C:\c\Users\...'` | 즉시 실패 |
| **10** | `fromisoformat`이 소수점 5자리 거부 (Python 3.9) | `ValueError` | 즉시 실패 |
| **11** | k6 원시 시계열 **347MB** | 저장소에 넣을 수 없다 | 파일 크기 |
| **12** | **동시 append 유실** — 백그라운드 k6와 부모 셸이 같은 파일에 덧붙임 | 종료 로그 확인 블록이 **통째로 사라짐** | "블록은 있는데 출력이 없다"는 모순 |
| **13** | **처치가 전달되지 않음** — `SERVER_SHUTDOWN`이 컨테이너에 미도달 | graceful/immediate **두 조건이 동일한 결과** | **측정 전 env 확인 가드** |

### ★ 즉시 실패 vs 조용히 틀린 결론 — 이 구분이 도구 설계 원칙이다

13건을 두 부류로 가르면 성격이 완전히 다르다.

| 부류 | 해당 | 성질 |
|---|---|---|
| **즉시 실패** | 9, 10, 11 | 실행이 멈춘다. **비용은 시간뿐이고 잘못된 결론으로 가지 않는다** |
| **조용히 틀린 결론** | 1, 3, 4, 5, 6, 7, 8, 12, **13** | 수치가 나온다. **그럴듯해서 그대로 문서에 박힐 수 있다** |

> **실패는 시끄러워야 한다.** 이 프로젝트가 도구에 넣어온 장치들 —
> `pyguard`의 표식 확인, `sweep.sh`의 파싱 실패 시 중단, `run.sh`의 `http_reqs` 검사,
> `retro-check.sh`의 대조군 강제, `shutdown-analyze.py`의 인플라이트 0 무효 판정,
> 그리고 이번 **측정 전 env 확인** — 은 전부 **두 번째 부류를 첫 번째 부류로 옮기는 장치**다.
>
> 13번이 그 값을 직접 보여줬다. **가드가 없었다면 "세 번째 독립 시도에서도 변별되지 않았다"를
> 문서에 박았을 것이고, 그 결론은 처치가 가해진 적 없는 실험에서 나온 것이었다.**
> 앞 턴에 "대조 실험에서는 처치가 가해졌다는 것 자체가 검증 대상"이라고 적고 넣은 가드가
> **바로 다음 실행에서 발동했다.**

### 12번은 Windows 전용 결함 네 번째다

`python3` 스텁 · `gradlew` CRLF · MSYS 경로 변환에 이어 **동시 append 유실**이 추가됐다.

| # | 결함 | 기제 |
|---|---|---|
| 1 | `python3`이 MS Store 스텁 | 앱 실행 별칭 |
| 2 | `gradlew` CRLF 셰뱅 | `core.autocrlf` |
| 3 | MSYS 경로 변환 | 유닉스 경로 자동 변환 |
| **4** | **동시 append 유실** | **MSYS의 `>>`가 O_APPEND 의미를 보장하지 않는다** — 자식이 자기 오프셋으로 써 내려가며 부모가 끼워 넣은 줄을 덮는다 |

**넷 다 macOS 여섯 Phase 동안 드러날 수 없었다.** 그리고 4번은 앞의 셋과 달리
**"실패가 성공처럼 보인다"가 아니라 "쓴 것이 사라진다"** 라 더 찾기 어려웠다 —
코드에 블록이 분명히 있는데 출력이 없었다.

### ★ 이 측정의 산출물은 graceful의 효과가 아니라 도구 결함 목록이다

그것 자체를 결과로 적는다. graceful shutdown을 재검증하려고 시작했는데
**나온 것의 대부분은 측정 도구의 결함**이었고, 그중 둘(12·13)은 **조용히 틀린 결론으로
직행할 수 있는 종류**였다.

Phase 3에서 *"이 Phase의 성격 — 측정이 결과물이다"* 라고 적었는데,
Phase 6의 이 구간은 한 걸음 더 갔다 — **측정 도구의 신뢰도가 결과물이다.**

**5번이 앞의 넷과 형태가 다르다.**

Phase 3에서 이렇게 적었다 — *"1·3·4는 개선 후였다면 성과처럼 보였을 것이다."*
그 관점은 **"틀린 값이 좋아 보이는 경우"** 를 경계한 것이다. 5번은 한 칸 더 나간다:

> **틀린 값이 좋아 보인 게 아니라, 틀린 계측이 "정확해 보였다."**
> `resp_2xx == http_reqs`라는 완전한 일치는 계측이 옳다는 신호로 읽혔다.
> 그래서 모순을 만났을 때 **의심의 방향이 k6로 갔고, 세 번을 헛돌았다.**

Phase 3의 방어("이상해 보이는 값에 센서를 단다")는 **이상해 보이지 않는 오류**를 잡지 못한다.
추가할 처방은 **"지표들 사이의 합이 맞는지"를 독립적으로 검산하는 것**이다 —
실제로 오류를 확정한 것도 `resp_2xx + resp_409 = http_reqs`가 맞아떨어지는지 본 순간이었다.

### 이 Phase가 산출한 것 — 튜닝값이 아니라 결함 1건

| 측정 | 결과 |
|---|---|
| 부하 (VU 30 / 60초) | 성공 100% · p95 7.16ms · 740 req/s |
| 동시 주문 정합성 (VU 50 / 2만 건) | **불일치 0** · 전부 성공 |
| 스파이크 (10 → 150 VU) | 성공 100% · p95 7.02ms · 회복됨 |
| 스트레스 (50 → 800 VU) | 성공 100% · p95 98ms · **한계점 미도달** |
| 설정 스윕 (풀·스레드) | 곡선 확보. **기본값 미변경** |
| **찾아낸 결함** | **동시 첫 주문의 제약 위반이 500으로 나가고 있었다** → 409로 교정 |

**부하 측정의 산출물이 성능 수치가 아니라 정확성 결함이었다.** 스트레스에서 한계점에 닿지도
못했으므로 "이 시스템의 상한"은 아직 모른다.

### 계약 준수

- [x] SPEC.md의 URI·JSON 필드 변경 없음
- [x] 새 응답 상태 추가 없음 — 제약 위반을 **기존** 409 `CONCURRENT_MODIFICATION`에 매핑했다.
      `SPEC.md` 4절의 "낙관적 락 충돌 → 409"가 이미 예고한 자리이며, 새 에러 코드를 만들지 않았다
- [x] 상품 상세에 `Cache-Control`·`ETag` 헤더 추가 — **바디는 그대로**이므로 계약 이탈이 아니다
      (Phase 5의 `X-Trace-Id`와 같은 판단)

### 절대 규칙

- [x] 엔티티 대신 **DTO를 캐시**한다 — 엔티티를 캐시하면 detached 인스턴스가 공유된다
- [x] 캐시·HTTP 캐시 설정이 `global/config`에 있고 도메인 경계를 넘지 않는다
- [ ] **근거 없는 설정값이 없는가 — ⚠️ 부분 미충족.**
      `max-age=60`은 도메인 판단으로 근거를 적었다(`DECISIONS.md` 25절).
      그러나 **풀 크기·스레드 수는 곡선만 있고 확정하지 않았다.**
      기본값을 바꾸지 않았으므로 "근거 없는 값이 들어간" 상태는 아니지만,
      **Phase 3에서 미룬 항목이 여전히 미결이다**

### 남은 문제

| 문제 | 상태 |
|---|---|
| **캐시·ETag의 성능 효과** | ❌ **미측정.** 구현·기능 검증만 완료 (`DECISIONS.md` 25절) |
| **튜닝값 확정 및 재측정** | ❌ 미완. 스윕 곡선만 있다 (`DECISIONS.md` 26절) |
| **HA 다중 인스턴스** | ❌ 미착수. Phase 5에서 이월된 항목 |
| **`phase0-baseline` vs 개선 후 비교표** | ❌ 미착수 |
| `graceful shutdown` k6 재검증 | ❌ 미착수 (Phase 5에서 미검증으로 남긴 항목) |
| 스트레스 한계점 | ⚠️ 800 VU에서도 미도달. 상한을 모른다 |
| 로컬 캐시의 다중 인스턴스 전파 | ⚠️ `ConcurrentMapCacheManager`는 인스턴스별이다. HA 구간에서 다룬다 |
| `GET /api/customers/list` 익명 노출 | ❌ 계약 유지 (Phase 2부터 이월) |
| 로그인 타이밍 부채널 / 요청 속도 제한 없음 | ❌ 미해결 |

### ★ 이 Phase에서 드러난 절차상의 결함 — 기록이 커밋 메시지에만 있었다

Phase 0~5는 전부 `JOURNAL`·`DECISIONS`·`REVIEW`에 기록이 남았다. **Phase 6만 없었다.**
발견 경위와 설계 근거가 **커밋 메시지에만** 존재했고, `docs/evidence/perf/README.md`는
*"상세는 `DECISIONS.md`·`JOURNAL.md`"* 라고 **존재하지 않는 문서를 가리키고 있었다.**

다른 머신에서 이어받으며 드러났다. 대화 맥락이 없는 상태에서 문서만 읽으면
**"측정은 했는데 왜 그렇게 했는지 알 수 없는" 상태**였다.

- **왜 생겼나** — Phase 6은 측정·수정·측정이 빠르게 반복돼(커밋 4개가 56분 안에) 기록 시점을 놓쳤다.
  커밋 메시지를 충실히 쓴 것이 오히려 "기록했다"는 감각을 줬다
- **왜 커밋 메시지로는 부족한가** — 커밋은 **결론을 적는 곳**이라 되돌린 시도와 시도 순서가 남지 않는다.
  실제로 "세 번 헛돌았다"는 남았지만 **그 세 번이 무엇이었는지는 복원할 수 없다**
- **조치** — 2026-08-08에 `DECISIONS.md` 24~26절, `JOURNAL.md` Phase 6 섹션, 이 절을 소급 작성했다.
  소요 시간은 복원할 수 없어 **커밋 간격 상한**으로만 적었다

> `CLAUDE.md`는 **"나중에 몰아서 쓰면 세부가 사라진다"** 고 적고 있다. 그 문장이 맞다는 것을
> 이번에 비용을 치르고 확인했다. 다음 Phase에서는 **측정 1회마다 일지 1항목**을 기준으로 삼는다.

### 문서 정정 — 이어받기에서 발견한 8건 (2026-08-08)

다른 머신에서 문서만 읽고 상태를 파악하다 나온 것들이다. **대화 맥락 없이 문서만 읽는 것이
이번의 점검 방식이었고, 그 조건이 아니면 안 보였을 항목들이다.**

| # | 무엇이 틀렸나 | 조치 |
|---|---|---|
| 1 | `REVIEW.md`에 Phase 6 절이 없음 | 이 절 신설 |
| 2 | `JOURNAL.md`에 Phase 6 절이 없음 | 항목 3건 소급 작성 |
| 3 | `DECISIONS.md`가 23절에서 끝남 | 24~26절 신설 |
| 4 | `evidence/perf/README.md`가 **존재하지 않는 문서**를 가리킴 | 절 번호까지 명시한 색인표로 교체 |
| 5 | `PLAN.md` Phase 6 체크박스가 전부 비어 있음 (4건은 완료) | 체크박스 갱신 + 계획에 없던 3항목 추가 |
| 6 | `PLAN.md` Phase 3의 "Phase 6으로 보류" 3건이 갱신 안 됨 | **기존 문장을 남기고** `→` 로 현재 상태를 덧붙임 |
| 7 | `README.md`가 "Phase 0이 끝나기 전이라 엔티티가 없다"고 안내 | **삭제** — 아래 참조 |
| 8 | `README.md` 프로파일 표의 `local` DB가 `5432` (실제·정책 모두 5433) | `5433`으로 정정 |

> **7번은 이 프로젝트에서 유일하게 "덧붙이지 않고 지운" 정정이다.** 이유를 남긴다.
>
> 그 문단은 설계 판단이 아니라 **동작 안내**였고, 내용은 *"앱이 아직 기동하지 않는다"* 였다.
> 6개 Phase 동안 사실이 아니었으며, 남겨두면 **처음 클론한 사람이 정상 동작을 실패로 오인한다.**
> 문단 자신이 *"Phase 0 완료 시 이 문단은 삭제한다"* 고 지시하고 있었고,
> Phase 5에서 세운 **"동작하지 않는 절차를 문서에 적는 것은 없는 것보다 나쁘다"** 와 같은 판단이다.
> 지운 사실과 원문 요지를 이 표에 남겨 기록은 보존한다.
>
> 8번도 성격이 같지만 한 단어 교체라 그대로 고쳤다. 특기할 것은 **`DECISIONS.md` 7절의 제목이
> "호스트 DB 포트를 5432로 노출하지 않는다"인데 README 표가 5432라고 말하고 있었다**는 점이다 —
> 설계 근거와 사용 안내가 정반대였고, 둘을 나란히 놓기 전에는 어느 쪽도 이상해 보이지 않는다.

---

## 머신 이전 검증 (2026-08-08) — macOS → Windows

Phase 0의 이식성 검증이 **"다른 물리 머신 — 미검증"** 으로 남겨둔 항목이 실제로 발생했다.
저장소를 새 머신에 클론해 이어서 작업했고, 그 과정 자체가 이식성 검증이 됐다.

### 검증 조건

| 항목 | 이전 (Phase 0~6) | 이번 |
|---|---|---|
| 호스트 | macOS Darwin 25.5.0 · **Apple Silicon(ARM)** | **Windows 10 Home 19045 · x86_64** |
| 컨테이너 런타임 | Docker Desktop 29.6.2 | Docker Desktop **29.1.2 · WSL2 백엔드** |
| 자원 | Docker VM 10 CPU · 7GB | **12 CPU · 7.7GiB** |
| 셸 | zsh/bash | **Git Bash (MinGW)** |
| JDK | 17 | 시스템 21 + `jdk-17.0.1` (툴체인이 후자를 선택) |
| 대상 | — | 커밋 `3f300ab` 상태의 클론 |

### 결과 — 최종적으로는 전부 통과했다

| 검증 | 결과 |
|---|---|
| 컨테이너 빌드 (`docker compose up --build`) | ✅ `skala-shop-api:local` Built |
| postgres → app 기동 | ✅ 둘 다 `healthy`. Phase 5의 `curl` 기반 HEALTHCHECK 정상 |
| `/actuator/health` | ✅ 200 `{"status":"UP","groups":["liveness","readiness"]}` |
| **`e2e.sh`** | ✅ **48/48** |
| **`./gradlew build`** | ✅ **테스트 163건 전부 통과**, 커버리지 게이트 포함, 2m53s |
| 바이트코드 타깃 | ✅ major 61 = **Java 17** (`JAVA_HOME`이 21인데도 툴체인이 17을 찾았다) |
| `pyguard-selftest.sh` | ✅ 13/13 |

**x86_64 아키텍처도 이번에 함께 닫혔다.** Phase 0에서 "ARM에서만 빌드했다"로 남긴 항목이다 —
`postgres:16-alpine`·`eclipse-temurin:17`의 멀티아치가 실제로 동작했다.

### ★ 그러나 "문서에 없는 조치"가 3건 필요했다 — 이것이 결함이다

Phase 0의 이식성 검증표에는 이런 줄이 있다.

| 확인 항목 | Phase 0 (macOS) | **이번 (Windows)** |
|---|---|---|
| README 실행 방법만으로 기동되는가 | 통과 | ❌ **실패** |
| 문서에 없는 수동 조치가 필요했는가 | **없음** | ❌ **3건 있었음** |

**Phase 0에서 세운 기준을 그대로 적용하면 이 3건은 전부 결함이다.**
그때 `container_name` 하드코딩 하나를 결함으로 판정한 것과 같은 잣대다.

| # | 문서에 없던 조치 | 증상 | 왜 문서로는 못 잡았나 |
|---|---|---|---|
| 1 | **Docker Desktop 수동 기동** | `docker compose` 가 데몬 연결 실패 | README는 "Docker(Compose v2 포함)만 있으면 된다"고만 적었다. macOS에서는 데몬이 상주하는 경우가 많아 "설치=실행 중"으로 읽힌다 |
| 2 | **`python3` 실인터프리터 확보** | `docs/verify/` 도구들이 **아무 일도 안 하고 종료 49** | MS Store 앱 실행 별칭이 `python3` 을 가로챈다. Windows 기본 설정이고 macOS에는 존재하지 않는 개념이다 |
| 3 | **`gradlew` 줄바꿈 정정** | 컨테이너 빌드가 `./gradlew: not found` (exit 127) | `core.autocrlf` 는 **머신 설정**이라 저장소가 강제하지 않으면 통제할 수 없었다. `.gitattributes` 가 없었다 |

**세 건 모두 "실행이 안 된다"가 아니라 "이상하게 실패한다"** 는 공통점이 있다.
2번은 성공처럼 조용히 끝나고, 3번은 파일이 있는데 `not found` 라고 말한다.
**메시지만 보고는 원인에 도달할 수 없는 형태**이며, 채점자가 만나면 그대로 막힌다.

### 조치

| # | 조치 | 커밋 |
|---|---|---|
| 1 | README에 Windows 전제 조건 절 신설 (아래 판단 참조) | 이번 |
| 2 | `pyguard.sh` — 호출 경계에서 인터프리터를 검사하고 시끄럽게 실패시킨다. 역검증 13건 | `a132952` |
| 3 | `.gitattributes` — `gradlew`·`*.sh` 를 `eol=lf` 로 고정. **설정이 아니라 저장소가 강제한다** | `54ddce0` |

2·3은 **이 머신을 고친 것이 아니라 저장소를 고친 것**이다. 환경만 고쳤다면 다음 머신에서 재발한다.

### ★ 이 두 결함은 여섯 Phase 동안 드러나지 않았다

`python3` 스텁도 `gradlew` CRLF도 **Windows에서만** 난다. Phase 0~6은 전부 macOS에서 진행됐고,
그 안에서는 어떤 테스트·검증으로도 재현될 수 없었다.

- **테스트 163건**이 잡지 못한다 — 애플리케이션 코드의 문제가 아니다
- **E2E 48건**도 잡지 못한다 — 앱이 뜬 뒤의 계약을 보는 도구다
- **ArchUnit·변이 46건**도 무관하다
- 잡을 수 있었던 유일한 수단은 **다른 OS에서 실제로 돌려보는 것**뿐이었다

> **"미검증 범위"를 표로 남겨둔 것이 실제로 결함을 찾아낸 사례다.**
>
> Phase 0에서 "다른 물리 머신 — 리스크 **낮음**"이라고 적었고 그 판정은 **틀렸다.**
> 그러나 **적어두었기 때문에** 머신을 옮길 때 "이건 예고된 미검증 구간"이라는 인식으로 접근했고,
> 문서에 없는 조치가 필요할 때마다 "환경 문제"로 넘기지 않고 **결함으로 셀 수 있었다.**
>
> 리스크 추정치가 틀린 것은 문제가 아니다. **추정치를 적지 않았다면 세 건 모두
> "새 컴퓨터라 원래 좀 손봐야지"로 지나갔을 것이다.**
> 미검증 목록의 값은 예측 정확도가 아니라 **나중에 대조할 기준을 남기는 데** 있다.

### 여전히 열려 있는 것

| 미검증 범위 | 상태 |
|---|---|
| ~~다른 물리 머신~~ | ✅ **닫힘** (이 절) |
| ~~x86_64 아키텍처~~ | ✅ **닫힘** (이 절) |
| **리눅스 호스트** | ❌ 열림. WSL2를 경유했을 뿐 호스트는 Windows다. 포트 바인딩 동작은 여전히 미검증 |
| **오프라인·폐쇄망** | ❌ 열림 (`DECISIONS.md` 22절 — 대비하지 않기로 판단) |
| 반복 기동 | ✅ 별도 확인됨 |

### 성능 기준값은 이관되지 않는다

`docs/evidence/perf/`의 수치는 **macOS·ARM·10CPU/7GB**에서 잰 것이다.
이 머신은 **Windows·x86_64·12CPU/7.7GiB이고 Docker가 WSL2를 경유해 I/O 특성이 다르다.**
**노이즈 차이가 아니라 다른 환경이므로 직접 비교하지 않는다.**

- 새 측정은 `win-before-*` 로 파일명을 구분한다. 기존 macOS 파일은 지우지 않는다
- **비교는 각 머신 안에서만** 한다. 보고서에서도 두 머신의 수치를 같은 표에 섞지 않는다
- **반복 편차를 이 머신에서 다시 잰다.** 이전 머신의 0.65ms를 그대로 쓰지 않는다 —
  WSL2 경유면 더 클 수 있고, **편차를 모르면 스윕 결과가 신호인지 노이즈인지 판정할 수 없다**
  (`DECISIONS.md` 26절의 원칙 그대로)

  > **[결과] 1.91ms — 이전 머신의 약 3배였다.** 그리고 이것이 앞선 결론 하나를 무효로 만들었다:
  > macOS에서 신호로 판정한 풀 스윕 폭 2.00ms가 **이 머신에서는 편차 안**이다.
  > 상세는 `DECISIONS.md` 26절 후속 항목.

### Windows 전용 결함 — 3건 누적

머신 이전 과정에서 **저장소 코드가 Windows에서만 깨지는 결함이 3건** 나왔다.
(위 "문서에 없던 조치" 표의 Docker Desktop 수동 기동은 성격이 다르다 — 그건 문서 누락이고,
아래 셋은 **저장소가 고쳐야 하는 결함**이다.)

| # | 결함 | 증상 | 조치 | 커밋 |
|---|---|---|---|---|
| 1 | `python3` 이 MS Store 스텁으로 잡힘 | 검증 도구가 **아무 일도 안 하고 exit 49** | `pyguard.sh` — 호출 경계에서 표식으로 확인 | `a132952` |
| 2 | `gradlew` CRLF 셰뱅 | 컨테이너 빌드 `./gradlew: not found` (127) | `.gitattributes` 로 `eol=lf` 고정 | `54ddce0` |
| 3 | **MSYS 경로 변환** | k6가 `/perf/load.js` 를 못 찾음 → **빈 표를 exit 0 으로 기록** | `MSYS_NO_PATHCONV` + `cygpath -m`, 파싱 실패 시 중단 | 이번 |

**셋의 공통 형태 — "실패가 성공처럼 보인다".**

| # | 겉보기 | 실제 |
|---|---|---|
| 1 | 출력 없음 = 문제 없음 | 스크립트가 실행되지 않았다 |
| 2 | 파일이 있는데 `not found` | 셰뱅에 `\r` 이 붙었다 |
| 3 | 표에 3줄이 찍히고 exit 0 | **측정기가 빈칸을 성공으로 기록했다** |

**셋 다 macOS 여섯 Phase 동안 드러날 수 없었다.** Windows 고유의 기제(앱 실행 별칭,
`core.autocrlf`, MSYS 경로 변환)에서 비롯되고, 이 저장소의 어떤 테스트도 —
163건·E2E 48건·ArchUnit·변이 46건 — 다른 OS의 셸 환경을 재현하지 않는다.

> **3번이 특히 이 프로젝트의 반복 주제와 맞닿는다.** `exit 0` 이 거짓말을 했다.
> `pyguard` 를 만들 때 **"종료 코드가 아니라 인터프리터가 직접 만든 표식을 받아야 한다"**
> 고 판단했는데, 같은 구조의 결함이 **측정 하네스에도 있었다** —
> 그때는 인터프리터의 생존을, 이번엔 측정의 성립을 종료 코드로 판단하고 있었다.
> 이제 둘 다 **산출물의 내용**으로 확인한다 (`PYGUARD_OK` 표식 / `http_reqs` 존재).

### 검증 도구를 고쳤으므로 그 사이 결과를 재검증했다

**절차** — Phase 1에서 검증 스크립트의 거짓 통과를 고친 뒤 커밋 5개를 되돌아가 18/18을
다시 돌린 것과 같은 절차다. **도구를 고치면 그 도구로 낸 과거 결과의 신뢰도가 함께 흔들린다.**

새로 넣은 가드와 **완전히 같은 패턴**을 `docs/evidence/perf/` 의 macOS 측정 결과에 적용했다.

| 검사 | 대상 | 결과 |
|---|---|---|
| ① 파싱 실패 (sweep 가드) | 원시 출력 11개 | ✅ 전부 통과 |
| ② `http_reqs` 존재 (run.sh 가드) | `before-*.txt` 4개 | ✅ 전부 존재 (20,151 / 66,968 / 74,662 / 980,722) |
| ③ 요약표 빈 칸 | 요약 3개 | ✅ 13줄 전부 채워짐 |
| ④ **JSON 독립 검산** | `before-*.json` 4개 | ✅ `http_reqs`·p95 가 `.txt` 와 일치 |

**④가 핵심이다.** ①~③은 **같은 텍스트를 같은 grep으로 다시 보는 것**이라,
그 grep이 틀렸다면 과거 파일에서도 똑같이 틀린다 — 자기 자신을 근거로 삼는 셈이다.
JSON 요약은 k6가 별도 경로로 내보낸 산출물이므로 **독립적인 검산**이 된다.

**검사기가 실패할 수 있는지도 확인했다.** 방금 실패한 측정 파일
(`sweep-win-repeat-FAILED-msys-pathconv.txt`)을 대조군으로 함께 넣었고 정확히 걸렸다.
이것이 없으면 "전부 ✅"는 **"검사기가 아무것도 잡지 못한다"** 와 구분되지 않는다.

오탐 1건 — 글롭(`sweep-*-*.txt`)이 하이픈 있는 라벨의 **요약 파일**까지 잡았다.
요약표에는 `checks_succeeded` 줄이 애초에 없으므로 당연히 파싱이 빈다. **데이터 문제가 아니다.**

> **이번 소급 검사의 값은 "숨은 오염을 찾아낸 것"이 아니다.**
> MSYS 경로 변환은 Windows 고유 기제라 macOS 측정 당시에는 **존재할 수 없었다** —
> 원리상 걸릴 것이 없는 검사였다.
> 값은 **"찾을 게 없다는 것을 근거를 갖고 말할 수 있게 된 것"** 에 있다.
> Phase 1의 재검증도 결과는 "과거의 통과는 실제로도 통과였다"였고,
> 그때도 값은 결과가 아니라 **확인했다는 사실** 자체였다.

---

## Phase 6 — HA 다중 인스턴스 검증 (2026-08-08)

Phase 5에서 이월돼 `PLAN.md`에 남아 있던 마지막 구조 항목이다.
구성 근거는 `DECISIONS.md` 27절, 측정 자료는 `docs/evidence/perf/win-ha-*`.

### 검증 결과 — 무상태가 처리량보다 먼저

| # | 검증 | 결과 |
|---|---|---|
| 1 | **분산** | ✅ 양쪽 수신. 단일 커넥션 기준 10/10 완전 교대 |
| 2 | **무상태** | ✅ 한 인스턴스에서 로그인 → **다른 인스턴스에서 주문 성공**, 잔액 정확, `X-Trace-Id` 유지 |
| 3 | **로컬 캐시** | ❌ **25절 예고대로 낡은 값 재현**(6/6). 영향은 조회 표시에 한정 — 주문 경로는 DB 직접 읽기 |
| 4 | **장애 내성** | ✅ 1대 정지 중 **60/60 성공, 실패 0** |
| 5 | **처리량** | ❌ **늘지 않고 줄었다** (1,003 → 814 req/s) |

**순서를 지킨 것이 값을 했다.** 1번(분산)을 먼저 보지 않았다면 이후 "동작한다"가
한쪽 인스턴스만으로 통과한 결과였을 수 있다 — 이 프로젝트가 반복해서 만난
"안 왔다를 막혔다로 읽는" 형태다.

### 계약 준수

- [x] SPEC.md의 URI·JSON 필드 변경 없음. **응용 코드를 한 줄도 바꾸지 않았다**
- [x] `docker compose up` 한 줄 실행 유지 — `docker-compose.yml` 무변경, HA는 독립 파일
- [x] 기본 구성에 `X-Upstream-Addr` **없음**(측정 시에만 opt-in). 실제로 없는 것을 확인

### 예고된 함정 — 하나는 발동, 하나는 발동 안 함

| 예고 | 출처 | 결과 |
|---|---|---|
| 로컬 캐시가 낡은 값을 준다 | `DECISIONS.md` 25절 | ✅ 그대로 발동 |
| `depends_on: service_healthy` 로 **무한 대기** | `JOURNAL` Phase 5 | ❌ **발동 안 함** — `compose exit=1`, 24초 오류 종료 |

**말로 두지 않고 app2가 healthy 가 될 수 없게 만들어 확인했다.**
예고가 틀렸다는 것도 검증의 산출물이다 — 확인하지 않았다면 틀린 문장이 남아
다음 사람을 잘못 대비시켰을 것이다. 다만 조건부로는 유효해서(확정까지 약 90초),
지우지 않고 **조건을 좁혀 남겼다.**

### 이 Phase에서 내가 틀린 것 2건

| # | 무엇 | 조치 |
|---|---|---|
| 1 | 진단하려고 컨테이너 안에서 `nginx -s stop` → **PID 1이 죽어 LB가 내려갔다** | 즉시 복구. 이후 설정으로 검증 |
| 2 | **HA 측정과 대조 측정 사이 DB 초기화 누락** — 대조가 오염된 상태로 결론 낼 뻔 | 폐기 후 각 측정 직전 초기화하여 재측정 |

2번은 Phase 3에서 겪고 `PLAN.md`에 경고까지 적어둔 함정을 반복한 것이다.
`sweep.sh`에는 초기화가 들어 있는데 **손으로 `docker run`을 부르며 그 경로를 비껴갔다** —
**도구의 안전장치를 우회하면 그 장치는 없는 것과 같다.**

### 사전 등록한 예측이 빗나갔고, 그 덕에 해석이 정정됐다

409 비율이 "풀 20 값(0.96%)에 근접"할 것이라고 측정 **전에** 기록해뒀다(`ha-prediction.md`).
실제는 **2.33%** — 예측 밴드 밖이다.

**`LB + 1대` 대조가 결정적이었다.** DB 동시성 폭이 단일과 동일한데 409가 **3.1배**(0.65 → 2.02%).
따라서 26절의 *"409는 DB 동시성 폭을 재는 **특이** 지표"* 라는 인과적 일반화를 **철회**했다.
대체 설명(지속시간 모델)도 스레드 스윕과 모순되어 성립하지 않는다.
**현재 기전을 설명하지 못하며, 미해결로 남겼다.**

> 예측을 측정 후에 적었다면 2.33%에 맞는 설명을 새로 붙였을 것이고,
> 26절의 과잉 일반화는 그대로 남았을 것이다.

### 남은 문제

| 문제 | 상태 |
|---|---|
| **DB 단일 지점** | ❌ 복제·페일오버 없음 |
| **LB 단일 지점** | ❌ nginx가 죽으면 접근 불가 |
| **`ddl-auto: create` 와 다중 인스턴스** | ❌ 한 인스턴스 재시작이 공유 DB를 초기화한다 → **롤링 재시작 불가** |
| 로컬 캐시 낡은 값 | ⚠️ 한계로 유지. `SPRING_CACHE_TYPE=none` 탈출구 제공(동작 확인) |
| nginx 워커별 `max_fails` | ⚠️ 차단기가 설정보다 약하다. 클라이언트 영향은 없었다 |
| 409 상승 기전 | ❌ **미해결** |
| 단일 머신에서의 수평 확장 이득 | ❌ **측정 불가** — 자원을 나눠 쓰므로 |

**"HA를 구현했다"가 아니라 "앱 계층의 수평 확장 가능성을 실증했다"가 정확하다.**
